apiVersion: aigateway.envoyproxy.io/v1alpha1
kind: BackendSecurityPolicy
metadata:
  name: dog-provider-policy
  namespace: default
spec:
  type: AWS_IAM
  awsSecurityPolicy:
    oidcCredential:
      oidc:
        provider:
          issuer: placeholder
        clientID: placeholder
        clientSecret:
          name: placeholder
      credentialFileName: placeholder
