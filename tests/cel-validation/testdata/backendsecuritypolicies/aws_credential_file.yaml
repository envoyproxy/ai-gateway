apiVersion: aigateway.envoyproxy.io/v1alpha1
kind: BackendSecurityPolicy
metadata:
  name: dog-provider-policy
  namespace: default
spec:
  type: AWS_IAM
  cloudProviderCredentials:
    awsCredentials:
      region: us-east-1
      credentialsFile:
        path: ~/.aws/placeholder
