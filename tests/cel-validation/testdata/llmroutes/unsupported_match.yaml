apiVersion: aigateway.envoyproxy.io/v1alpha1
kind: LLMRoute
metadata:
  name: apple
  namespace: default
spec:
  schema:
    name: OpenAI
  rules:
    - matches:
      - headers:
        - type: RegularExpression
          name: x-envoy-ai-gateway-model
          value: llama3-70b
      backendRefs:
        - name: kserve
          weight: 20
        - name: aws-bedrock
          weight: 80
