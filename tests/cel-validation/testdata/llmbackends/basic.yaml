apiVersion: aigateway.envoyproxy.io/v1alpha1
kind: LLMBackend
metadata:
  name: dog-backend
  namespace: default
spec:
  schema:
    schema: AWSBedrock
  backendRef:
    name: dog-service
    kind: Service
    port: 80
