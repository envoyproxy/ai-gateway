# Copyright Envoy AI Gateway Authors
# SPDX-License-Identifier: Apache-2.0
# The full text of the Apache license is available in the LICENSE file at
# the root of the repo.

# Valid: overwrite false with a header-injected API key (secretRef).
apiVersion: aigateway.envoyproxy.io/v1beta1
kind: MCPRoute
metadata:
  name: backend-apikey-overwrite-false
  namespace: default
spec:
  parentRefs:
    - name: some-gateway
      kind: Gateway
      group: gateway.networking.k8s.io
  backendRefs:
    - name: mcp-service
      kind: Service
      port: 80
      securityPolicy:
        apiKey:
          secretRef:
            name: github-sa-token
          overwrite: false
      forwardHeaders:
        - name: X-GitHub-PAT
          backendHeader: Authorization
